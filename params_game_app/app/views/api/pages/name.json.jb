{
  message: "name prints here", # is message, here, a method? If so, where is it established? We didn't write the method ...
# Regardless, we can point to/run other elements?
# Maybe it's an hash, given the curly braces and the symbol + string ...
}
