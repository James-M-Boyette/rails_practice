@products.map do |product|
  {
    id: product.id,
    name: product.name,
    price: product.price,
  # stock: product.stock,
  # description: product.description,
  # image_url: product.image_url,
  # is_dicountined: product.is_dicountined,
  }
end

#Note: need to add stock, description, image_url and is_discountined to table
